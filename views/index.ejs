<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <div class="wrapper">
        <% if(typeof(errors) != 'undefined' ) { %>
            <% for (var x in errors) { %>
             <h3><%= errors[x].message %></h3>
            <% } %>
        <% } %>

        <h1>Welcome to Quoting Dojo</h1>
        <form action="/quotes" method="POST">
            <label for="name">Your Name:</label><input type="text" name="name">
            <br>
            <label for="quote">Your Quote:</label>
            <br>
            <textarea name="quote" id="quote" cols="60" rows="10"></textarea>
            <br>
            <button type="submit">Add my quote!</button>
        </form>

        <a href="/quotes"><button type="submit">Skip to Quotes</button></a>
    </div>
</body>
</html>